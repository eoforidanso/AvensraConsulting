Static design preview of the Emmanus Plus Consulting website, generated
from github.com/eoforidanso/AvensraConsulting for GitHub Pages.

GitHub Pages serves static files only, so:
  - Buy buttons and forms display correctly but cannot submit.
  - /admin, /access and /order/confirmed are excluded (they need a backend).
  - The contact form's ?topic= pre-fill does not apply here.

NOTE: the repo and this URL still carry the previous brand name; renaming
the repo would change this URL. See LAUNCH-CHECKLIST.md 1.12.

The full functional site needs a Node host — see README.md.
